<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.0</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>

	<groupId>com.kafka</groupId>
	<artifactId>events-parent</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<packaging>pom</packaging>
	<name>events-parent</name>
	<description>Reusable Kafka client starter plus a demo application that exercises it</description>

	<modules>
		<module>kafka-events-starter</module>
		<module>events-app</module>
	</modules>

	<properties>
		<java.version>25</java.version>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<!--
		  Modern JDKs no longer run annotation processors found on the classpath unless asked.
		  Without this, spring-boot-configuration-processor stays silent and consumers of the
		  starter get no IDE completion or documentation for the kafka.* keys.
		-->
		<maven.compiler.proc>full</maven.compiler.proc>
	</properties>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>com.kafka</groupId>
				<artifactId>kafka-events-starter</artifactId>
				<version>${project.version}</version>
			</dependency>
		</dependencies>
	</dependencyManagement>

</project>
